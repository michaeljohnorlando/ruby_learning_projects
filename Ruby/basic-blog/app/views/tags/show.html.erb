<h1> Articles with tag = <%= @tag.name %> </h1>

<ul>
    <% @tag.articles.each do |article| %>
        <li> <%= link_to article.title, article_path(article) %> </li>
    <% end %>
    <p><%= link_to "Delete Tag", tag_path(@tag),
data: {confirm: "Are you shure you want to delete this TAG?"},method: :delete %></p>
</ul>

<footer><%= link_to "<<<back to articles list", articles_path %></footer>